# Text-to-Image Sample

Demonstrates text-to-image generation using Stable Diffusion v1.4 from the Model Garden.

## Prerequisites

- .NET 10.0 SDK

## Run

```bash
dotnet run
```

The model (~4 GB) will be downloaded automatically on first run and cached for subsequent runs.
