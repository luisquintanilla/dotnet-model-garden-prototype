# Segment Anything Sample

Demonstrates promptable image segmentation using the SAM2 Hiera-Tiny model from the Model Garden.

## Prerequisites

- .NET 10.0 SDK
- A test image file named `test-image.jpg` in the working directory

## Run

```bash
dotnet run
```

The model (~126 MB) will be downloaded automatically on first run and cached for subsequent runs.
