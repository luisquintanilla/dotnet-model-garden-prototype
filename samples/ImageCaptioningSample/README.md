# Image Captioning Sample

Demonstrates image captioning using the GIT-Base (COCO) model from the Model Garden.

## Prerequisites

- .NET 10.0 SDK
- A test image file named `test-image.jpg` in the working directory

## Run

```bash
dotnet run
```

The model (~707 MB) will be downloaded automatically on first run and cached for subsequent runs.
