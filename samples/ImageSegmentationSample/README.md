# Image Segmentation Sample

Demonstrates semantic image segmentation using the SegFormer-B0 model from the Model Garden.

## Prerequisites

- .NET 10.0 SDK
- A test image file named `test-image.jpg` in the working directory

## Run

```bash
dotnet run
```

The model (~15 MB) will be downloaded automatically on first run and cached for subsequent runs.
