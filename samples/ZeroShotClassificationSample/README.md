# Zero-Shot Classification Sample

Demonstrates zero-shot image classification using the CLIP model from the Model Garden.

## Prerequisites

- .NET 10.0 SDK
- A test image file named `test-image.jpg` in the working directory

## Run

```bash
dotnet run
```

The model (~608 MB) will be downloaded automatically on first run and cached for subsequent runs.
