# Visual QA Sample

Demonstrates visual question answering using the GIT-Base (TextVQA) model from the Model Garden.

## Prerequisites

- .NET 10.0 SDK
- A test image file named `test-image.jpg` in the working directory

## Run

```bash
dotnet run
```

The model (~709 MB) will be downloaded automatically on first run and cached for subsequent runs.
