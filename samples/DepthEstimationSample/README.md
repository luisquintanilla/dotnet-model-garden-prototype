# Depth Estimation Sample

Demonstrates monocular depth estimation using the DPT-Hybrid model from the Model Garden.

## Prerequisites

- .NET 10.0 SDK
- A test image file named `test-image.jpg` in the working directory

## Run

```bash
dotnet run
```

The model (~533 MB) will be downloaded automatically on first run and cached for subsequent runs.
